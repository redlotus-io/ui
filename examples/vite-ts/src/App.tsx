import { RealButton } from "@redlotus/ui";

export const App = () => {
  return (
    <div>
      <header>
        <p>Hello Vite + React!</p>
        <RealButton>Toggle</RealButton>
      </header>
    </div>
  );
};
